import {integer,sqliteTable,text,uniqueIndex} from "drizzle-orm/sqlite-core";
export const templates=sqliteTable("templates",{id:text("id").primaryKey(),code:text("code").notNull(),name:text("name").notNull(),category:text("category").notNull(),status:text("status").notNull().default("draft"),version:integer("version").notNull().default(1),configJson:text("config_json").notNull(),createdAt:text("created_at").notNull(),updatedAt:text("updated_at").notNull()},t=>[uniqueIndex("templates_code_idx").on(t.code)]);
export const templateVersions=sqliteTable("template_versions",{id:text("id").primaryKey(),templateId:text("template_id").notNull(),version:integer("version").notNull(),configJson:text("config_json").notNull(),publishedAt:text("published_at").notNull()});
export const productBindings=sqliteTable("product_bindings",{id:text("id").primaryKey(),shopifyProductId:text("shopify_product_id").notNull(),productTitle:text("product_title").notNull(),productHandle:text("product_handle"),templateId:text("template_id").notNull(),publishedVersion:integer("published_version"),createdAt:text("created_at").notNull(),updatedAt:text("updated_at").notNull()});
