import {db,ensureDb,serialize,type TemplateRow} from "../../../../../lib/store";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{await ensureDb();const {id}=await params;const body=await request.json() as {code:string;name:string;category:string;config:{steps?:unknown[]}};if(!body.name?.trim()||!body.code?.trim())return Response.json({error:"模板名称和编码不能为空"},{status:400});if(!body.config?.steps?.length)return Response.json({error:"至少需要一个定制步骤"},{status:400});const current=await db().prepare("SELECT * FROM templates WHERE id=?").bind(id).first<TemplateRow>();if(!current)return Response.json({error:"Template not found"},{status:404});const version=current.version+1,now=new Date().toISOString(),json=JSON.stringify(body.config);await db().batch([db().prepare("UPDATE templates SET code=?,name=?,category=?,status='published',version=?,config_json=?,updated_at=? WHERE id=?").bind(body.code,body.name,body.category,version,json,now,id),db().prepare("INSERT INTO template_versions VALUES (?,?,?,?,?)").bind(`${id}-v${version}`,id,version,json,now)]);const row=await db().prepare("SELECT * FROM templates WHERE id=?").bind(id).first<TemplateRow>();return Response.json({success:true,data:serialize(row!)})}catch(e){return Response.json({error:e instanceof Error?e.message:"Publish failed"},{status:400})}}
